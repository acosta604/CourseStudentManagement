@RestController
@RequestMapping("/notes")
public class GradeController {
    @Autowired
    private NoteRepository noteRepository;

    // Define CRUD operations using appropriate HTTP methods
}
